import {prisma} from "../lib/prisma.ts";

export const deposit = async (walletId: number, amount: number, reference?: string) => {
    
}

export const transfer = async (fromWalletId: number, toWalletId: number, amount: number, reference?: string) => {

}

export const findAll = async (walletId?: number) => {

}